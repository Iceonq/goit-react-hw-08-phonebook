export const HomePage = () => {
  return (
    <div>This is homepage</div>
    // <div>
    //   <Form />
    //   <ContactsList />
    //   {isLoading && !error && <h4>Loading...</h4>}
    //   {error && <h4>{error}</h4>}
    // </div>
  );
};
